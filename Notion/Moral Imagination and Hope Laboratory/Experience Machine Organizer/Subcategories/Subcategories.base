# Subcategories

filters:
  and:
    - note["base"] == link("Subcategories.base")
properties:
  file.name:
    displayName: Name
views:
  - type: table
    name: Table View
    order:
      - file.name
